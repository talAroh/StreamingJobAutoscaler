[pytest]
testpaths = autoscaler_service/tests
filterwarnings =
    ignore::DeprecationWarning:botocore.*
    # Starlette 1.6 still imports the deprecated `anyio.abc.BlockingPortal` alias in its
    # own testclient module; it is not something this project's code can change.
    ignore:The anyio.abc.BlockingPortal alias is deprecated:DeprecationWarning:starlette.testclient
